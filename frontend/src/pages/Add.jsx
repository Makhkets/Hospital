import { useEffect } from "react";
import { useNavigate } from "react-router-dom";

const Add = () => {
    // const navigate = useNavigate();
    // useEffect(() => {
    //     return navigate("/")
    // })

    return (
        <div>
            <h1>Add</h1>
        </div>
    );
};

export default Add